# ARCHITECTURE

## Обзор

Wildberries MCP Server - это Model Context Protocol сервер для интеграции с Wildberries API. Проект предоставляет стандартизированный доступ к функциональности Wildberries через MCP инструменты, позволяя AI-ассистентам и другим MCP-клиентам взаимодействовать с платформой продавца.

## Контекст

**Цель проекта:** Создать полноценный MCP сервер, который обеспечивает:
- Управление товарами (получение, обновление контента, цен, остатков)
- Работу с заказами FBS (получение, обработка, обновление статусов)
- Доступ к аналитике (поисковые запросы, история остатков, воронка продаж)

**Целевая аудитория:** Продавцы на Wildberries, использующие AI-ассистенты для автоматизации работы.

## Ключевые компоненты

### 1. MCP Server Core (`src/index.ts`)
- Инициализация MCP сервера через stdio transport
- Регистрация всех инструментов
- Обработка запросов `ListTools` и `CallTool`
- Маршрутизация вызовов к соответствующим обработчикам

### 2. Конфигурация (`src/config/`)
- **env.ts**: Валидация переменных окружения (WB_API_TOKEN) через Zod
- **config.ts**: Централизованная конфигурация (API URLs, rate limits, retry настройки)

### 3. Wildberries Client (`src/client/`)
- **wildberries-client.ts**: Базовый HTTP клиент с axios
- **auth.ts**: Авторизация через API токен
- **rate-limiter.ts**: Контроль частоты запросов (100 req/min по умолчанию)
- **error-handler.ts**: Единая обработка ошибок API с retry логикой

### 4. Tools Layer (`src/tools/`)

Модульная структура инструментов по разделам API:

**Товары** (`products/`):
- `wb_get_products` - получение списка товаров
- `wb_update_product_content` - обновление контента
- `wb_update_product_price` - управление ценами
- `wb_update_product_stock` - управление остатками

**Заказы FBS** (`orders/fbs/`):
- `wb_get_orders_fbs` - получение заказов
- `wb_get_picking_tasks_fbs` - сборочные задания
- `wb_update_order_status_fbs` - обновление статусов

**Аналитика** (`analytics/`):
- `wb_get_search_queries` - поисковые запросы
- `wb_get_stock_history` - история остатков
- `wb_get_sales_funnel` - воронка продаж

### 5. Type System (`src/types/`)
- **api.ts**: Типы для Wildberries API (Products, Orders, Analytics)
- **tools.ts**: Типы для MCP инструментов, схемы валидации Zod
- **common.ts**: Общие типы (ApiError, RateLimitError, PaginatedResponse)

### 6. Utilities (`src/utils/`)
- **logger.ts**: Winston логирование с ротацией файлов
- **validator.ts**: Валидация данных через Zod
- **formatter.ts**: Форматирование ответов для MCP

## Потоки данных

```
MCP Client (AI Assistant)
    ↓ [MCP Request]
MCP Server Core (src/index.ts)
    ↓ [Tool Name + Params]
Tool Handler (src/tools/*/handler.ts)
    ↓ [Validate Params]
Validator (src/utils/validator.ts)
    ↓ [Validated Data]
Wildberries Client (src/client/wildberries-client.ts)
    ↓ [HTTP Request + Rate Limiting]
Wildberries API
    ↓ [HTTP Response]
Error Handler / Formatter
    ↓ [Formatted Response]
MCP Client
```

### Обработка ошибок

1. Ошибки API перехватываются в `wildberries-client.ts`
2. Конвертируются в типизированные `ApiError` через `error-handler.ts`
3. Применяется retry логика для временных ошибок (5xx, timeout, network)
4. Ошибки форматируются через `formatter.ts` и возвращаются клиенту

### Rate Limiting

1. Глобальный `RateLimiter` отслеживает запросы в скользящем окне
2. Перед каждым запросом проверяется доступность слота
3. При превышении лимита запрос ожидает освобождения слота
4. Поддержка экспоненциального backoff для retry

## Технологии и зависимости

### Runtime
- **Node.js 18+**: Современные ES modules, async/await
- **TypeScript 5.7+**: Строгая типизация, безопасность типов

### Core Dependencies
- **@modelcontextprotocol/sdk (^1.0.4)**: MCP protocol implementation
- **axios (^1.7.9)**: HTTP клиент для API запросов
- **zod (^3.24.1)**: Валидация данных и схем
- **winston (^3.17.0)**: Структурированное логирование
- **dotenv (^16.4.7)**: Управление переменными окружения

### API Endpoints
- Content API: `https://content-api.wildberries.ru`
- Marketplace API: `https://marketplace-api.wildberries.ru`
- Statistics API: `https://statistics-api.wildberries.ru`
- Suppliers API: `https://suppliers-api.wildberries.ru`

## Нефункциональные требования и ограничения

### Производительность
- Время отклика < 1 секунда для стандартных операций
- Поддержка пагинации для больших наборов данных (до 1000 записей)
- Кэширование не реализовано (статeless сервер)

### Надёжность
- Автоматический retry до 3х попыток для временных ошибок
- Экспоненциальный backoff (1s, 2s, 4s)
- Graceful shutdown при получении SIGTERM/SIGINT
- Логирование всех операций для отладки

### Безопасность
- API токен хранится в `.env` (не коммитится в git)
- Автоматическое удаление токенов из логов
- Валидация всех входных параметров через Zod
- Изоляция процесса через stdio transport

### Масштабируемость
- Stateless архитектура (можно запустить несколько экземпляров)
- Модульная структура инструментов (легко добавлять новые)
- Rate limiting защищает от превышения лимитов API

### Ограничения
- Wildberries API rate limit: ~100 запросов/минуту
- Максимальный размер ответа: не ограничен (зависит от API)
- Timeout запросов: 30 секунд (для обычных), 120 секунд (для загрузки)
- Нет поддержки websockets / real-time обновлений

## Roadmap

### ✅ MVP (Completed)
- [x] Базовая инфраструктура MCP сервера
- [x] Wildberries HTTP клиент с rate limiting
- [x] Инструменты для товаров (get, update content/price/stock)
- [x] Инструменты для заказов FBS (get orders/tasks, update status)
- [x] Инструменты для аналитики (search queries, stock history, sales funnel)
- [x] Документация (README, API reference, examples)

### 🔄 Phase 2 (Planned)
- [ ] Тестирование с реальным API ключом
- [ ] Unit и integration тесты
- [ ] Инструменты для заказов DBW/DBS/Самовывоз
- [ ] Инструменты для маркетинга и продвижения
- [ ] Инструменты для отчётов

### 🔮 Phase 3 (Future)
- [ ] Поддержка поставок FBW
- [ ] Работа с тарифами
- [ ] Документы и бухгалтерия
- [ ] Общение с покупателями (вопросы, отзывы, чаты, возвраты)
- [ ] Wildberries Цифровой
- [ ] Кэширование часто запрашиваемых данных
- [ ] Метрики и мониторинг (Prometheus/Grafana)
- [ ] Docker контейнеризация
