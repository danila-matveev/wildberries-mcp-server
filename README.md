# Wildberries MCP Server

Model Context Protocol сервер для интеграции с Wildberries API.

## 🚀 Возможности

### Товары
- Получение списка товаров с фильтрацией и пагинацией
- Обновление контента товаров (название, описание, характеристики)
- Управление ценами и скидками
- Управление остатками на складах

### Заказы FBS
- Получение списка заказов за период
- Работа со сборочными заданиями
- Обновление статусов заказов

### Аналитика
- Статистика поисковых запросов
- История остатков товаров
- Воронка продаж (просмотры → корзина → заказы → выкупы)

## 📋 Требования

- Node.js 18+
- API токен Wildberries

## 🔧 Установка

1. Клонируйте репозиторий:

```bash
git clone <repository-url>
cd wildberries-mcp-server
```

2. Установите зависимости:

```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

4. Добавьте ваш API токен Wildberries в `.env`:

```env
WB_API_TOKEN=your_actual_token_here
```

### Получение API токена

1. Войдите в личный кабинет продавца Wildberries
2. Перейдите в **Настройки → API**
3. Выберите **"Для интеграции вручную"**
4. Создайте **тестовый токен** (для разработки)
5. Выберите **все категории доступа**
6. Установите уровень доступа: **"Чтение и запись"**
7. Скопируйте токен (он показывается только один раз!)

## 🛠️ Сборка

```bash
npm run build
```

## ▶️ Запуск

После сборки запустите сервер:

```bash
npm start
```

## 📦 Использование в MCP клиенте

Добавьте сервер в конфигурацию MCP клиента:

```json
{
  "mcpServers": {
    "wildberries": {
      "command": "node",
      "args": ["/path/to/wildberries-mcp-server/dist/index.js"]
    }
  }
}
```

## 🔑 Доступные инструменты

### Товары

#### `wb_get_products`
Получить список товаров

**Параметры:**
- `limit` (опц.): количество товаров (макс. 1000, по умолчанию 100)
- `offset` (опц.): смещение для пагинации
- `search` (опц.): поиск по названию
- `brandID` (опц.): фильтр по ID бренда
- `updatedAtFrom` (опц.): дата обновления от (ISO 8601)
- `updatedAtTo` (опц.): дата обновления до (ISO 8601)

#### `wb_update_product_content`
Обновить контент товара

**Параметры:**
- `nmID`: артикул WB товара
- `title` (опц.): новое название
- `description` (опц.): новое описание
- `characteristics` (опц.): массив характеристик

#### `wb_update_product_price`
Обновить цену товара

**Параметры:**
- `nmID`: артикул WB товара
- `price`: новая цена (в рублях)
- `discount` (опц.): скидка в процентах (0-100)

#### `wb_update_product_stock`
Обновить остатки товара

**Параметры:**
- `nmID`: артикул WB товара
- `warehouseID`: ID склада
- `quantity`: количество товара

### Заказы FBS

#### `wb_get_orders_fbs`
Получить заказы FBS

**Параметры:**
- `dateFrom`: дата начала периода (ISO 8601)
- `dateTo`: дата окончания периода (ISO 8601)
- `status` (опц.): фильтр по статусу (new, confirm, complete, cancel, cancel_by_client)
- `take` (опц.): количество заказов (макс. 1000, по умолчанию 100)
- `skip` (опц.): пропустить N заказов

#### `wb_get_picking_tasks_fbs`
Получить сборочные задания FBS

**Параметры:**
- `dateFrom`: дата начала периода (ISO 8601)
- `dateTo`: дата окончания периода (ISO 8601)
- `status` (опц.): фильтр по статусу

#### `wb_update_order_status_fbs`
Обновить статус заказа FBS

**Параметры:**
- `orderUID`: уникальный ID заказа
- `status`: новый статус (new, confirm, complete, cancel, cancel_by_client)

### Аналитика

#### `wb_get_search_queries`
Получить поисковые запросы

**Параметры:**
- `dateFrom`: дата начала периода (ISO 8601)
- `dateTo`: дата окончания периода (ISO 8601)
- `nmID` (опц.): артикул WB товара для фильтрации

#### `wb_get_stock_history`
Получить историю остатков

**Параметры:**
- `dateFrom`: дата начала периода (ISO 8601)
- `dateTo`: дата окончания периода (ISO 8601)
- `nmID`: артикул WB товара

#### `wb_get_sales_funnel`
Получить воронку продаж

**Параметры:**
- `dateFrom`: дата начала периода (ISO 8601)
- `dateTo`: дата окончания периода (ISO 8601)
- `nmID` (опц.): артикул WB товара для фильтрации

## 📚 Документация

- [Справочник API](docs/api-reference.md) - подробное описание всех инструментов
- [Примеры использования](docs/examples.md) - практические примеры
- [Архитектура](../ARCHITECTURE.md) - архитектура проекта

## 🔒 Безопасность

- API токен хранится в `.env` файле (не коммитится в git)
- Токен автоматически удаляется из логов
- Все запросы логируются без чувствительных данных

## 🐛 Отладка

Логи сохраняются в папке `logs/`:
- `combined.log` - все логи
- `error.log` - только ошибки

Уровень логирования можно настроить через переменную окружения `LOG_LEVEL` (error, warn, info, debug).

## 📝 Лицензия

MIT

## 🤝 Вклад

Contributions are welcome! Please feel free to submit a Pull Request.

## 📧 Контакты

- Документация Wildberries API: https://dev.wildberries.ru/
- Swagger: https://dev.wildberries.ru/swagger
