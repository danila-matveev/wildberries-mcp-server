# 🚀 Быстрый старт

## Что это?

**Wildberries MCP Server** - это сервер для интеграции с Wildberries API через Model Context Protocol. Позволяет AI-ассистентам управлять товарами, заказами и получать аналитику.

## ⚡ Запуск за 5 минут

### 1. Установите зависимости

```bash
npm install
```

### 2. Получите API токен

1. Войдите в [личный кабинет продавца Wildberries](https://seller.wildberries.ru/)
2. **Настройки** → **API**
3. **"Для интеграции вручную"**
4. Создайте **тестовый токен** (для разработки)
5. Выберите **все категории доступа**
6. Уровень доступа: **"Чтение и запись"**
7. **Скопируйте токен** (показывается только один раз!)

### 3. Настройте `.env`

```bash
cp .env.example .env
```

Откройте `.env` и добавьте ваш токен:

```env
WB_API_TOKEN=ваш_токен_здесь
```

### 4. Соберите проект

```bash
npm run build
```

### 5. Запустите сервер

```bash
npm start
```

✅ Готово! Сервер запущен и готов принимать запросы через MCP.

---

## 📚 Что дальше?

### Документация

- **[README.md](README.md)** - полная документация
- **[docs/api-reference.md](docs/api-reference.md)** - справочник всех 10 инструментов
- **[docs/examples.md](docs/examples.md)** - примеры использования
- **[ARCHITECTURE.md](ARCHITECTURE.md)** - архитектура проекта

### Доступные инструменты (10)

**Товары (4):**
- `wb_get_products` - получение списка товаров
- `wb_update_product_content` - обновление контента
- `wb_update_product_price` - управление ценами
- `wb_update_product_stock` - управление остатками

**Заказы FBS (3):**
- `wb_get_orders_fbs` - получение заказов
- `wb_get_picking_tasks_fbs` - сборочные задания
- `wb_update_order_status_fbs` - обновление статусов

**Аналитика (3):**
- `wb_get_search_queries` - поисковые запросы
- `wb_get_stock_history` - история остатков
- `wb_get_sales_funnel` - воронка продаж

### Использование в MCP клиенте

Добавьте в конфигурацию вашего MCP клиента:

```json
{
  "mcpServers": {
    "wildberries": {
      "command": "node",
      "args": ["/путь/к/wildberries-mcp-server/dist/index.js"]
    }
  }
}
```

---

## 🐛 Проблемы?

### Ошибка: "WB_API_TOKEN is required"
- Проверьте, что файл `.env` создан
- Убедитесь, что токен добавлен в `.env`

### Ошибка: "Cannot find module"
- Запустите `npm install`
- Проверьте, что Node.js версии 18 или выше

### Ошибка: "Authentication failed"
- Проверьте корректность API токена
- Убедитесь, что токен не истёк

### Логи

Все логи сохраняются в папке `logs/`:
- `logs/combined.log` - все события
- `logs/error.log` - только ошибки

---

## 📞 Помощь

- [Документация Wildberries API](https://dev.wildberries.ru/)
- [Swagger API](https://dev.wildberries.ru/swagger)
- [Model Context Protocol](https://modelcontextprotocol.io/)
